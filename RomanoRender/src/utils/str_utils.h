#pragma once

#include <string>

// function that removes spaces within a char*
void remove_spaces(char* s);

// function that increment a string name by 1
std::string increment_name(std::string& name);